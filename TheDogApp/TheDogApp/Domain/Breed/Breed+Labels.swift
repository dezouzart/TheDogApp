import Foundation

extension Labels {
    enum Breed {
        enum Erros {
            static let title = "Ops"
            static let fetchMessage = "Could not load breeds from server"
        }
    }
}
